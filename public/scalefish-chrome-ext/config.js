// 扩展配置：修改下面两个地址后重新加载扩展即可
const SCALEFISH_CONFIG = {
  // 后端 API 地址（对应网站的 /api 前缀，不要以 / 结尾）
  BACKEND_URL: 'https://scalefish.example.com/api',
  // 网站主页地址（未登录时会打开此页面引导用户登录）
  HOME_URL: 'https://scalefish.example.com',
}
